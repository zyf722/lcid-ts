name = "lcid-ts"
main = "src/index.ts"
compatibility_date = "2024-03-20"
compatibility_flags = ["nodejs_compat"]

[triggers]
crons = ["0 0 * * *"]  # Runs at 00:00 UTC every day

[[kv_namespaces]]
binding = "<binding>"  # The binding ID of the KV namespace
id = "<id>"  # The ID of the KV namespace
